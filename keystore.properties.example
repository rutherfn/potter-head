# Copy to keystore.properties (gitignored) for local signed release/stage bundles.
# CI uses GitHub Secrets instead — see .github/workflows/android_app_bundle_release.yml
#
# Your upload keystore (no file extension):
storeFile=/Users/nicholasrutherford/Desktop/tys
storePassword=
keyAlias=
keyPassword=
